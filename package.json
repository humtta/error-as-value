{
  "name": "error-as-value",
  "description": "Handle errors as values",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",

  "scripts": {
    "format": "biome format --write .",
    "lint": "biome check ."
  },

  "devDependencies": {
    "@biomejs/biome": "^2.5.13"
  }
}
