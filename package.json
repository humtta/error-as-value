{
  "name": "error-as-value",
  "description": "Handle errors as values",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",

  "scripts": {
    "format": "biome format --write .",
    "lint": "biome check .",
    "check": "npm run format && npm run lint"
  },

  "devDependencies": {
    "@biomejs/biome": "^2.5.13",
    "typescript": "^7.0.2"
  }
}
